#pragma once
#include <string>
using namespace std;

namespace string_utils
{
    string lstrip(const string &, char);
    string rstrip(const string &, char);
    string strip(const string &, char);
}